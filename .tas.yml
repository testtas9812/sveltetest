framework: mocha
tier: large
postMerge:
  pattern: 
    - "test/test.ts"
preMerge:
  pattern:
    - "test/test.ts"
preRun:
     command:
        - npm install
postRun:
     command:
        - node --version
        - node --version
version: 1.0

